# copy to .env for local dev, adjust if the backend runs elsewhere
VITE_API_URL=http://localhost:5000/api

# on vercel set VITE_API_URL in the project settings to the backend
# project url plus /api, for example https://your-backend.vercel.app/api
